## Git Safety

NEVER silently resolve merge conflicts — flag all conflicts with before/after context and get user approval.

NEVER stash or discard uncommitted changes without showing the user what would be lost.

NEVER force-push without explicit user approval. Warn if target is main/master.

NEVER amend published commits without user approval — prefer new commits over amends.
