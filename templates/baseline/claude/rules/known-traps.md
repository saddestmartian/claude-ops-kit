## Known Traps (Auto-loaded)

<!-- This file accumulates platform, framework, and API-specific gotchas
     discovered during development. Each entry should document:
     1. What fails and how (the symptom)
     2. Why it fails (the root cause)
     3. What to do instead (the fix)

     Add entries here when you discover a non-obvious failure mode —
     especially ones where the code appears to work (no errors) but
     produces wrong behavior at runtime.

     Example entry:

     ### UIListLayout sorts alphabetically by default
     Without explicit LayoutOrder, children render alphabetically by Name,
     not insertion order. Always set SortOrder = LayoutOrder on the layout
     and LayoutOrder = index on each child.
     Caught in session S63: items rendered "Today, This Month, This Week".
-->
