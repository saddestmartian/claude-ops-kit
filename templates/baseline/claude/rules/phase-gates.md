## Phase Gates

NEVER batch multiple module changes into a single silent stretch when a task spans 3+ files or 2+ modules.

Gate sequence: read/plan → report findings → implement first module → report → get confirmation → continue. Do not skip gates.
